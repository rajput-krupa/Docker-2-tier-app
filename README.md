# Docker-2-tier-app
2-tier Dockerized Flask application with Flask and MySQL running in isolated containers, connected via a custom Docker network. Includes persistent database storage using Docker volumes, an optimized multi-stage Dockerfile, and images published to Docker Hub for easy deployment.
